# Copy to .env and fill in values

# PostgreSQL
DB_PASSWORD=changeme

# Session cookie secret (min 32 chars, random)
SESSION_SECRET=change-me-to-a-random-32-char-string!!

# CSRF protection key (min 32 chars, random)
CSRF_AUTH_KEY=change-me-to-another-32-char-string!

# App public URL (used for CSRF trusted origins)
APP_BASE_URL=https://hakken.example.com

# Set to true in production (HTTPS)
SECURE_COOKIES=true

# Docker host path for database volume (production)
DOCKER_ROOT=/opt/docker

# Optional: cover image providers (leave blank to use Wikipedia fallback only)
# Free key at pexels.com/api
PEXELS_API_KEY=
# Free key at unsplash.com/developers
UNSPLASH_ACCESS_KEY=

# Directory for locally stored uploaded images (default: data/uploads)
UPLOADS_DIR=data/uploads
